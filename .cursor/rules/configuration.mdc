---
description: 
globs: 
---
{
  "id": "004",
  "name": "Code Block Integrity",
  "description": "Ensure that the code blocks have the appropriate language tags and file path metadata when updating existing files.",
  "self_modifiable": true,
  "global_configuration": {
    "selfModification": {
      "allowed": true,
      "instructions": "These structured .mdc rules can be modified by editing the individual rule files in the .cursor/rules/ directory as needed."
    }
  }
}