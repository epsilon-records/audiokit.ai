[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
name = "audiokit-ai"
version = "0.1.0"
description = "Backend for AudioKit-AI Server"
authors = ["Your Name <youremail@example.com>"]
dependencies = [
    "fastapi",
    "uvicorn",
    "celery",
    "redis",
    "pydantic",
    "PyJWT",
    "fastapi-limiter",
    "python-multipart",
    "httpx",
    "asyncpg",
]

[tool.hatch.metadata]
requires = [] 