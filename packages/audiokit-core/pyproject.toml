[tool.poetry]
name = "audiokit-core"
version = "0.1.0"
description = "Shared models and interfaces for AudioKit ecosystem"
authors = ["Your Name <your.email@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
pydantic = "^2.6.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api" 